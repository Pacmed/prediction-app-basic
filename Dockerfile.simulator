FROM python:3.7

SHELL ["/bin/bash", "-c"]

# Copy the complete repository to the /www folder
COPY . /www

# Upgrade pip and install the required packages
RUN pip install --upgrade pip
RUN pip install -r /www/requirements.txt

CMD ["python", "/www/simulator.py"]
